# Review: plan-glm-20260830-1029.md

| field | value |
|---|---|
| reviewer | `glm` |
| backend | `opencode` |
| model | `zai-coding-plan/glm-5.3` |
| workdir | `/home/v/Documents/Dev/rkt-simulator` |
| write access | none (read-only, enforced) |
| started | 2026-08-30T10:29:01+02:00 |
| finished | 2026-08-30T10:34:56+02:00 |
| exit code | 0 |

---

VERDICT: APPROVE WITH CHANGES

- BLOCKER - plan.md:Task 6 Step 8 (`simulation.ts` `step()`, ground clamp) - The clamp `if (s.position.y <= environment.groundHeight)` runs every tick from ignition. Position starts exactly at `groundHeight`, and the thrust shape rises linearly from zero, so tick 1 has thrust ≈ 0.27 N vs 0.743 N weight (test config): velocity and position go negative on the first tick, the clamp fires, and the rocket is set to `phase='landed'`, `outcome='nominal'` at t = 1/120 s. Every flight "lands" at ignition. Task 6 Step 9's phase-order and apogee tests fail despite "Expected: PASS" (the 'lands back near ground' and 'deterministic' tests pass trivially, masking it), and the Task 18 smoke test's "altitude rose above 0" fails. Fix: clamp only after apogee, e.g. `if (s.phase === 'descent' && s.position.y <= environment.groundHeight)`, keeping the clamp/outcome logic unchanged; note in the plan that a few-mm pad dip during the ~3-tick thrust ramp is expected. (Verified the fix yields apogee ≈ 270 m for the test C6 config by hand integration — inside the 30–600 m test band.)
- MINOR - plan.md:Task 6/types + spec.md:§7.1 - `Motor.delayS` is dead data: the chute deploys at the exact apogee tick, while the spec says "chute deploy after ejection delay". Either deploy at the first descent tick after `burnout + delayS`, or delete `delayS` and amend the spec.
- MINOR - plan.md:Task 3 + spec.md:§6.1, Task 9 data - Spec says the curve is "generated from `avgThrustN` and `burnTimeS`", but peak is derived solely from `totalImpulseNs/(burnTimeS·SHAPE_MEAN)`; `avgThrustN` never shapes thrust (only feeds `tipOffProbability`). Data self-inconsistency: A8-3 impulse/burn = 5.0 N ≠ `avgThrustN` 8 N (curve peak 6.25 N is below the "8" in its name). Fix the A8-3 row and/or the spec wording.
- MINOR - plan.md:Task 19 Step 3 vs Task 16 - "Observe CATO with an overloaded motor" is impossible through the UI: the motor select is filtered to `compatibleMotors`, and no catalog rocket recommends a motor exceeding its `maxMotorImpulseNs` (checked all 12). Add a dev-only "all motors" selector or drop the manual step.
- MINOR - plan.md:Task 18 Step 2 - Smoke procedure polls the HUD "for ~5 s" but asserts altitude "returns toward 0"; chute descent from ~250–300 m at ~3.4 m/s takes 60–90 s. Assert only "rose above 0" (optionally wait up to ~120 s for the summary modal).
- MINOR - plan.md:Task 16 Files list - `src/ui/format.ts` is tested (Step 1) and created (Step 3) but missing from the Files:Create list (the `git add src/ui` in Step 6 does cover it). Add it for traceability.
- MINOR - plan.md:Task 14 `explode()` - The "explosion burst" creates `THREE.Points` with an all-zero position buffer, never animated or faded: it renders as one static dot at the rocket position and lingers until `dispose()`. Spread the positions in a small sphere at minimum, ideally animate/fade ~1 s.

## Assumptions and gaps

- Not executed (no code exists yet); findings are from inspection only. The Task 6 blocker was verified by hand-computing tick 1 (thrust 7.81·(t/1.6/0.15) ≈ 0.27 N at t = 1/120 vs weight 0.743 N), not by running.
- Task 7's "deploys the chute at apogee in the nominal case" passes iff `mulberry32(1)()` ≥ 0.04 (4% chute-fail roll); deterministic, but I could not evaluate mulberry32's actual first draw by hand. Same for Task 1's "different seeds diverge". Both are fixed-seed so they either always pass or always fail — run once to confirm.
- Could not verify npm version compatibility (`three ^0.169` + `@types/three`, `vite ^5.4`, `vitest ^2.1`) or the `three/examples/jsm/controls/OrbitControls.js` import path against current published packages; the plan already directs the implementer to check.
- Brave binary path `/usr/bin/brave-browser-stable` and available CDP/Playwright tooling in the execution environment are unverified.
- Determinism was checked by inspection and no leak found: sim randomness uses only the seeded RNG (2 draws/tick via `windAt`, 1–2 at ignition, 1 at apogee, in fixed order); `Math.random` appears only in `world/effects.ts` (cosmetic) and `Date.now()` only for the per-launch seed in `main.ts`; the accumulator fixes step count independent of frame rate.
- Task 6 Step 9's forward dependency on Task 7's `applyOutcome` is acknowledged in the plan and excluded per review instructions; with a temporary no-op, Task 6 tests still fail on the blocker above.
- Task 11's six unshown environment builders and Task 16 Step 5's `Ui` class body are intentional deferrals; their correctness is not reviewable.
